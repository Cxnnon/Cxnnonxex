name: Ping Replit Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *'  # ทำงานทุกๆ 5 นาที

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Replit
      run: curl -s https://your-replit-project-url.repl.co
